<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trabfinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrabFinal">
        <activity
            android:name=".View.GamesListActivity"
            android:exported="false"
            android:label="@string/title_activity_games_list"
            android:theme="@style/Theme.TrabFinal.NoActionBar" />
        <activity
            android:name=".View.GamePageActivity"
            android:exported="false"
            android:label="@string/title_activity_game_page"
            android:theme="@style/Theme.TrabFinal.NoActionBar" />
        <activity
            android:name=".View.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>